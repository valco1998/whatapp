import type { OutputKind } from '../../types.mts'
export declare function handleDiffScan({
  after,
  before,
  depth,
  file,
  orgSlug,
  outputKind
}: {
  after: string
  before: string
  depth: number
  file: string
  orgSlug: string
  outputKind: OutputKind
}): Promise<void>
//# sourceMappingURL=handle-diff-scan.d.mts.map
