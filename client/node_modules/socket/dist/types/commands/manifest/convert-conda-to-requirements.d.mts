import type { CResult } from '../../types.mts'
export declare function convertCondaToRequirements(
  target: string,
  cwd: string,
  verbose: boolean
): Promise<
  CResult<{
    contents: string
    pip: string
  }>
>
export declare function convertCondaToRequirementsFromInput(
  input: string
): string
//# sourceMappingURL=convert-conda-to-requirements.d.mts.map
