import type { OutputKind } from '../../types.mts'
export declare function handleListRepos({
  direction,
  orgSlug,
  outputKind,
  page,
  per_page,
  sort
}: {
  direction: string
  orgSlug: string
  outputKind: OutputKind
  page: number
  per_page: number
  sort: string
}): Promise<void>
//# sourceMappingURL=handle-list-repos.d.mts.map
